<%=
  create_window(
    :name => 'filter_rule',
    :title => @rule.name,
    :content => render(
      :partial => 'rule_form',
      :locals => {
        :rule => @rule,
        :identifier => @identifier,
        :form_url => apply_filter_rule_admin_service_groups_path
      }
    ),
    :auto_open => true
  )
%>

setTimeout(function(){$('#value').focus().select();}, 500);