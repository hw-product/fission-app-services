%div.panel.panel-default
  %div.panel-body
    - if rule.description
      %div.well.well-sm= rule.description
    = bootstrap_form_tag :url => form_url, :remote => true do |f|
      = f.text_field :value
      = f.hidden_field :rule_id, :value => rule.id
      = f.hidden_field :identifier, :value => identifier
      = f.submit 'Add', :disable_with => 'Add...', :class => 'btn btn-success pull-right'
