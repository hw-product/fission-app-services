<%=
  create_window(
    :name => 'service_edit',
    :title => "Edit Config: #{@service.display_name.humanize}",
    :size => 'normal',
    :content => render(
      :partial => 'service_edit',
      :locals => {
        :service => @service,
        :config => @config
      }
    )
  )
%>
