<%=
  create_window(
    :name => 'service_preview',
    :title => "Service Config: #{@service.name.humanize}",
    :size => 'large',
    :content => render(
      :partial => 'service_preview',
      :locals => {
        :service => @service,
        :config => @config
      }
    )
  )
%>
